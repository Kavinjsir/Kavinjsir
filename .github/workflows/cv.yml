on:
  push:
    branches:
      - cv
jobs:
  cv-job:
    runs-on: ubuntu-latest
    name: Create cover letter
    steps:
      - uses: actions/checkout@main
      - name: Build pdf from tex
        id: build
        uses: Kavinjsir/latex-compiler@v1.7
      - name: View output
        run: ls /github/workspace
